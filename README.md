Used Text to column to separate the data into readable format.
Filtered the columns and removed blanks from the Income column.
Used Proper() to standardize the Marital status and ducation
Removed duplicates using Remove Duplicates (There were no duplicates).
Manually edited the data to rename the Headings to all lower cases.
I Replaced YOLO with Single, Absurd with Single using ctrl + H in the Marital status column.
I replaced - with / in the date column and used Ctrl + 1 , chose custom and yy/mm/dddd to change the date format.
